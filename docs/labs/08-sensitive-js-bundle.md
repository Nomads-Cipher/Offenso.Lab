## 08 - Sensitive values in client JavaScript

### Goal
Locate internal endpoints and keys in client-side JavaScript.

### File
- `GET http://localhost:3000/static/js/main.chunk.js`

### Steps
1. Open DevTools → Sources (or Network).
2. Open `main.chunk.js`.
3. Search for `INTERNAL_ENDPOINTS` and `apiKey`.

